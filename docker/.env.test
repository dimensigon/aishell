# AI-Shell Test Environment Configuration
# This file contains environment variables for the Docker Compose test setup

# ============================================================================
# MongoDB Configuration
# ============================================================================
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=MyMongoPass123
MONGO_DATABASE=testdb

# MongoDB Express (Admin UI)
MONGO_EXPRESS_USER=admin
MONGO_EXPRESS_PASSWORD=pass

# ============================================================================
# Redis Configuration
# ============================================================================
# Redis runs without authentication by default
# Connection: localhost:6379

# ============================================================================
# PostgreSQL Configuration
# ============================================================================
POSTGRES_USER=postgres
POSTGRES_PASSWORD=MyPostgresPass123
POSTGRES_DB=testdb

# PostgreSQL connection string:
# postgresql://postgres:MyPostgresPass123@localhost:5432/testdb

# ============================================================================
# MySQL Configuration (External Container)
# ============================================================================
# Note: AI-Shell connects to an external MySQL container named 'tstmysql'
# The external container should have these credentials:
MYSQL_ROOT_PASSWORD=MyMySQLPass123
MYSQL_DATABASE=testdb
MYSQL_PORT=3307
MYSQL_HOST=localhost

# MySQL connection string (external container 'tstmysql'):
# mysql://root:MyMySQLPass123@localhost:3307

# ============================================================================
# Oracle Database 23c Free Configuration (External Container)
# ============================================================================
# Note: AI-Shell connects to an external Oracle container named 'tstoracle'
# The external container should have these credentials:
ORACLE_PASSWORD=MyOraclePass123
ORACLE_SID=FREE
ORACLE_PDB=FREEPDB1
ORACLE_PORT=1521
ORACLE_HOST=localhost

# Oracle connection strings (external container 'tstoracle'):
# CDB: SYS/MyOraclePass123@//localhost:1521/FREE as SYSDBA
# PDB: SYS/MyOraclePass123@//localhost:1521/FREEPDB1 as SYSDBA

# ============================================================================
# Network Configuration
# ============================================================================
COMPOSE_PROJECT_NAME=aishell-test

# ============================================================================
# Resource Limits (Optional Override)
# ============================================================================
# Uncomment to override default resource limits
# MONGODB_MEMORY_LIMIT=2G
# REDIS_MEMORY_LIMIT=1G
# POSTGRES_MEMORY_LIMIT=2G
# MYSQL_MEMORY_LIMIT=2G
# ORACLE_MEMORY_LIMIT=4G

# ============================================================================
# Connection Summary
# ============================================================================
# MongoDB:    mongodb://admin:MyMongoPass123@localhost:27017/testdb
# Redis:      redis://localhost:6379
# PostgreSQL: postgresql://postgres:MyPostgresPass123@localhost:5432/testdb
# MySQL:      mysql://root:MyMySQLPass123@localhost:3307 (external 'tstmysql' container)
# Oracle CDB: SYS/MyOraclePass123@//localhost:1521/FREE as SYSDBA (external 'tstoracle' container)
# Oracle PDB: SYS/MyOraclePass123@//localhost:1521/FREEPDB1 as SYSDBA (external 'tstoracle' container)
#
# Admin UIs:
# Adminer (SQL):        http://localhost:8080
# Mongo Express:        http://localhost:8081 (user: admin, pass: pass)
# Redis Commander:      http://localhost:8082
